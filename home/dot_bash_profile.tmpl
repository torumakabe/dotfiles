{{ if ne .chezmoi.os "windows" -}}
# bash は ~/.bash_profile / ~/.bash_login のいずれかが存在すると ~/.profile を
# 読まないため、共通 env (~/.profile) を明示的に source する。
# 対話 bash が読む ~/.bashrc は dot_bashrc.tmpl で管理する。
[ -r "$HOME/.profile" ] && . "$HOME/.profile"
{{ end -}}
