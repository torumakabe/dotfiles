# Copilot Instructions (User-Level)

## ツール操作の制約（デフォルト動作より優先）

- Python → `python` / `pip` の直接実行は禁止。以下を使うこと:
  - スクリプト実行: `uv run script.py`（依存は PEP 723 で宣言）
  - パッケージ追加: `uv add <pkg>`（プロジェクト）/ `uv pip install <pkg>`（グローバル）
  - REPL: `uv run python`
- Windows: ファイルをエディタで開く → `Start-Process edit <path>`、Markdown 閲覧 → `Show-Markdown -Path <path> -UseBrowser`
- Windows: Copilot CLI のシェルセッションでは `$PROFILE` が読み込まれないため、エイリアスや関数は使えない
- macOS / Linux: コマンド存在チェック → `command -v`（`which` は使わない）
- コード/テキスト検索は組み込み `grep` / `glob` を使う。`rg` / `Select-String` / `findstr` をシェル経由で叩かない（ヒット全文がコンテキストに残り再送され続けるため）

## 言語

- 日本語で応答する（文脈上明らかに英語が必要な場合を除く）
- コードコメント・ドキュメント・コミット説明・PR 本文: README の言語に従う
- コミットメッセージの Conventional Commits プレフィックスとブランチ名は英語（URL/CLI 互換性）
- 「ベストプラクティス」は生成文に使わない（検索クエリは除く）。「推奨事項」「定番の方法」等で代替
- 「寄せる」を安易に使わない（特に GPT-5 系で多用しがち）。物理的に物を近づける意味なら可。それ以外（合わせる・統一する・準拠する・近づける・倣う等）は具体的な動詞で言い換える

## 図解

- Markdown で図解する場合、コードブロックには Mermaid を使い、図解の構成・粒度は C4 model に従う

## エージェント行動規範

- **コミット / push / PR 作成**: 明示的な指示があるまで実行しない。編集後は `git diff --stat` と要約を提示して承認を待つ
- **ブランチ**: ファイル変更を伴うタスクで main ブランチにいる場合、作業開始前にフィーチャーブランチを作成する。ブランチ名はタスク内容から自動生成し、作成時に報告する
- **分析の前提確認**: 日時・対象リソース・データ範囲など、分析の前提条件を先に明示してからユーザーと合意を取る。前提を確認せず結論を出さない

## コンテキスト / コスト最適化

設計方針: 自己監視前提のルールは守れないため、**数値・状態トリガー**と、必ず発火する動作（`report_intent` でのフェーズ切替報告）への**フック**で表現する。

### 調査の委譲（数値トリガー）

- 同一タスクで **3 ファイル以上を横断**して調べる、または **1 ファイルで 200 行超**を読む見込みの調査は、自分で `view` / `grep` を積まず `task`（explore）に委譲し、メインには要約だけ残す。`web_fetch` でのページ全文取得も委譲側で行う
- 既知の 1〜2 ファイルを読む・特定シンボルを 1 回引く程度は委譲しない（自分でやる方が速い）

### サブエージェントのモデル既定（既定＋例外形）

- `task` 起動時の `model` 既定は `claude-sonnet-4.6`。単純な列挙・抽出・読解のみなら `claude-haiku-4.5` / `gpt-5-mini` を明示指定する
- `opus` 系を使うのは「設計レビュー / 複雑なバグ解析」と判断した時のみ。使う場合は理由を 1 行添える。`opus` 系（特に `xhigh`）をサブエージェントの既定にしない

### 永続化の前確認（禁止形）

- 成果物をファイルに書く前に、会話で要約を提示し保存先を確認する。無断でファイルを増やさない

### コンテキスト管理（イベントフック化）

- `report_intent` でフェーズを切り替えて報告するたびに、必ず次の 1 行を添える:
  `コンテキスト判断: <継続 / compact推奨 / new推奨 / model昇格推奨> — <一言理由>`
- `new推奨` を出すときは `@path` での参照引き継ぎ手順も併記する
- `model昇格推奨` は現モデルでは不足と判断したとき。`/model` で `claude-opus-4.7` 系への昇格を促す
- ターン数を自分で数えない。フェーズ切替という必ず起きる事象に判断を紐づける

## コーディング規約

- Python 対象バージョン: 3.14+
- プライマリクラウド: Azure
